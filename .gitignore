node_modules/
dist/
.DS_Store
.claude/
Video/
*.local
# SQLite DB lives only at the repo root; do NOT use bare `data/` because
# it would also ignore src/games/cluepaw/data/cases.js and Railway breaks.
/data/
.env
